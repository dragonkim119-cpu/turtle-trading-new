{
  "name": "turtle-trading-new",
  "private": true,
  "scripts": {
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "backtest": "tsx scripts/backtest.ts",
    "backtest:sweep": "tsx scripts/sweep.ts",
    "backtest:crossval": "tsx scripts/crossval.ts"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
